# Generate with: openssl rand -hex 32
SESSION_SECRET=replace-with-a-long-random-secret
# Optional comma-separated Wake-on-LAN directed broadcasts, for example:
# WOL_BROADCASTS=192.168.1.255,10.0.0.255
# Optional comma-separated UDP ports. Defaults to 9,7.
# WOL_PORTS=9,7
