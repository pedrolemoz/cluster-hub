services:
  cluster-hub:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      SESSION_SECRET: ${SESSION_SECRET:?Set SESSION_SECRET in your .env file}
    volumes:
      - cluster-hub-data:/app/data
    restart: unless-stopped

volumes:
  cluster-hub-data:
